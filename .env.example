# GeonicDB の接続先（当日配布するカードの値をそのまま入れる）
VITE_GEONICDB_URL=https://geonicdb.geolonia.com
VITE_GEONICDB_TENANT=

# Geolonia Maps の API キー。
# YOUR-API-KEY は localhost / GitHub Pages / Vercel / Netlify / Cloudflare Pages で
# そのまま動く開発用キーなので、ワークショップ中は変更不要。
VITE_GEOLONIA_API_KEY=YOUR-API-KEY
