# @geolonia/geonicdb-sdk が optional peer として宣言している @geolonia/embed の
# バージョン範囲 (^1.0.0) が現行 5.x より古く、素の npm install が ERESOLVE で失敗する。
# 実行時は互換（embed 5 系も Map / Marker / geolonia を export している）ため、
# 解決だけ緩める。SDK 側の範囲が直ったらこのファイルは削除してよい。
legacy-peer-deps=true
